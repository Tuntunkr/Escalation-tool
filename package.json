{
  "name": "escalation-portal",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:web": "pnpm --filter @escalation/web dev",
    "dev:api": "pnpm --filter @escalation/api dev",
    "build": "pnpm -r run build",
    "build:api": "pnpm db:generate && pnpm --filter @escalation/api build",
    "build:web": "pnpm --filter @escalation/web build",
    "start:api": "pnpm --filter @escalation/api start",
    "start:api:prod": "pnpm db:push && pnpm --filter @escalation/api start",
    "start:web": "pnpm --filter @escalation/web start",
    "db:generate": "pnpm --filter @escalation/db generate",
    "db:push": "pnpm --filter @escalation/db push",
    "db:seed": "pnpm --filter @escalation/db seed",
    "db:studio": "pnpm --filter @escalation/db studio"
  },
  "engines": {
    "node": ">=20"
  }
}
